#!/bin/sh
# postinst script for neo4j
#
# see: dh_installdeb(1)

set -e

# summary of how this script can be called:
#        * <postinst> `configure' <most-recently-configured-version>
#        * <old-postinst> `abort-upgrade' <new version>
#        * <conflictor's-postinst> `abort-remove' `in-favour' <package>
#          <new-version>
#        * <postinst> `abort-remove'
#        * <deconfigured's-postinst> `abort-deconfigure' `in-favour'
#          <failed-install-package> <version> `removing'
#          <conflicting-package> <version>
# for details, see http://www.debian.org/doc/debian-policy/ or
# the debian-policy package


case "$1" in
    configure)


        # Create @@MYAPP_USER@@ user if it doesn't exist.
        if ! id @@MYAPP_USER@@ > /dev/null 2>&1 ; then
            adduser --system --home /opt/@@MYAPP_APP@@ --no-create-home \
                --ingroup nogroup --disabled-password --shell /bin/bash \
                @@MYAPP_USER@@
        fi

        chown @@MYAPP_USER@@:adm /var/lib/@@MYAPP_APP@@ /var/log/@@MYAPP_APP@@
        chmod 750           /var/lib/@@MYAPP_APP@@ /var/log/@@MYAPP_APP@@

		chown -R @@MYAPP_USER@@:adm /opt/@@MYAPP_APP@@

        chown -R @@MYAPP_USER@@:adm /var/run/@@MYAPP_APP@@
        chmod -R 750           /var/run/@@MYAPP_APP@@

		chown -R @@MYAPP_USER@@:adm /var/@@MYAPP_APP@@
	    chmod -R 750           /var/@@MYAPP_APP@@

		if [ ! -d @@APP_TMPDIR@@ ]; then
			mkdir @@APP_TMPDIR@@
			chown -R @@MYAPP_USER@@:adm @@APP_TMPDIR@@
			chmod -R 750            @@APP_TMPDIR@@
    	fi


		if [ ! -e /opt/@@MYAPP_APP@@/logs ]; then
			ln -s /var/log/@@MYAPP_APP@@ /opt/@@MYAPP_APP@@/logs
			#chown @@MYAPP_USER@@:adm /opt/@@MYAPP_APP@@/logs
		fi

		if [ ! -e /opt/@@MYAPP_APP@@/work ]; then
			ln -s /var/@@MYAPP_APP@@ /opt/@@MYAPP_APP@@/work
			#chown @@MYAPP_USER@@:adm /opt/@@MYAPP_APP@@/temp

		fi

		if [ ! -e /opt/@@MYAPP_APP@@/temp ]; then
			ln -s @@APP_TMPDIR@@ /opt/@@MYAPP_APP@@/temp
			#chown @@MYAPP_USER@@:adm /opt/@@MYAPP_APP@@/temp
		fi




    ;;

    abort-upgrade|abort-remove|abort-deconfigure)
    ;;

    *)
        echo "postinst called with unknown argument \`$1'" >&2
        exit 1
    ;;
esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0


