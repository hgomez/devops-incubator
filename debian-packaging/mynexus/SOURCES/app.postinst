#!/bin/sh
# postinst script for neo4j
#
# see: dh_installdeb(1)

set -e

# summary of how this script can be called:
#        * <postinst> `configure' <most-recently-configured-version>
#        * <old-postinst> `abort-upgrade' <new version>
#        * <conflictor's-postinst> `abort-remove' `in-favour' <package>
#          <new-version>
#        * <postinst> `abort-remove'
#        * <deconfigured's-postinst> `abort-deconfigure' `in-favour'
#          <failed-install-package> <version> `removing'
#          <conflicting-package> <version>
# for details, see http://www.debian.org/doc/debian-policy/ or
# the debian-policy package


case "$1" in
    configure)
    
	    
        # Create @@SKEL_USER@@ user if it doesn't exist.
        if ! id @@SKEL_USER@@ > /dev/null 2>&1 ; then
            adduser --system --home /opt/@@SKEL_APP@@ --no-create-home \
                --ingroup nogroup --disabled-password --shell /bin/bash \
                @@SKEL_USER@@
        fi
      
        chown @@SKEL_USER@@:adm /var/lib/@@SKEL_APP@@ /var/log/@@SKEL_APP@@
        chmod 750           /var/lib/@@SKEL_APP@@ /var/log/@@SKEL_APP@@

		chown -R @@SKEL_USER@@:adm /opt/@@SKEL_APP@@

        chown -R @@SKEL_USER@@:adm /var/run/@@SKEL_APP@@
        chmod -R 750           /var/run/@@SKEL_APP@@

		chown -R @@SKEL_USER@@:adm /var/@@SKEL_APP@@
	    chmod -R 750           /var/@@SKEL_APP@@
	
		if [ ! -d @@APP_TMPDIR@@ ]; then
			mkdir @@APP_TMPDIR@@
			chown -R @@SKEL_USER@@:adm @@APP_TMPDIR@@
			chmod -R 750            @@APP_TMPDIR@@
    	fi
		

		if [ ! -e /opt/@@SKEL_APP@@/logs ]; then
			ln -s /var/log/@@SKEL_APP@@ /opt/@@SKEL_APP@@/logs
			#chown @@SKEL_USER@@:adm /opt/@@SKEL_APP@@/logs
		fi
		
		if [ ! -e /opt/@@SKEL_APP@@/work ]; then
			ln -s /var/@@SKEL_APP@@ /opt/@@SKEL_APP@@/work
			#chown @@SKEL_USER@@:adm /opt/@@SKEL_APP@@/temp
				
		fi
		
		if [ ! -e /opt/@@SKEL_APP@@/temp ]; then
			ln -s @@APP_TMPDIR@@ /opt/@@SKEL_APP@@/temp
			#chown @@SKEL_USER@@:adm /opt/@@SKEL_APP@@/temp
		fi
		
	
		
       
    ;;

    abort-upgrade|abort-remove|abort-deconfigure)
    ;;

    *)
        echo "postinst called with unknown argument \`$1'" >&2
        exit 1
    ;;
esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0


